<%= form_with(model: annotation) do |form| %>

  <% if annotation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(annotation.errors.count, "error") %> prohibited this annotation from being saved:</h2>
      <ul>
        <% annotation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <div class="field">
      <%= form.label :body %>
      <%= form.text_area :body %>
    </div>
  </div>

  <div class="field">
    <div class="field">
      <%= form.label :friend_id %>
      <%= form.collection_select(:friend_id, @friends, :id, :name, include_blank: "Choix..." )  %>
    </div>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>

<% end %>
